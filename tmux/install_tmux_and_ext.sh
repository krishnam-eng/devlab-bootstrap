# I am currently using both ubuntu and centos. So, you will find commands for both here.

# Install tmux on Ubuntu / Debian
sudo apt-get update
sudo apt-get install tmux

# Install tmux on Fedora / CentOS
sudo yum -y install tmux

#todo: add cmds of used plugins setup
