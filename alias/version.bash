#!/usr/bin/env bash

# global alias to get version. e.g. python V => python --version
alias -g V="--version"

# todo: the tools that does_t support --version are handful, let's handle them differently
# tmux -V
# iostat -V
# try using hist expansion || !:0 -V

