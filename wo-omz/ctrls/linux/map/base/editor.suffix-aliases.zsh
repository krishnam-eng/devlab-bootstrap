#########==========---------
# Suffix Aliases
#
#   Suffix Aliases are defined using the -s flag. With suffix aliases, you can launch files with a specific extension (or suffix) in your favorite tool.
#########==========---------

# markdown files
alias -s md=code

# Python files
alias -s py=code

# shell
# alias -s {sh,bash,zsh}=nano

# run commands file
alias -s {zshrc,nanorc,bashrc}=vim

# config files
alias -s {config,conf}=vim

# valina text
alias -s {txt}=nano
