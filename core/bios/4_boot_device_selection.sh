# The BIOS/UEFI determines the boot order, which specifies the sequence in which it searches for a bootable operating system.
function main() {
  _determine_boot_order
}

function _determine_boot_order() {
        cd ../grub
}
