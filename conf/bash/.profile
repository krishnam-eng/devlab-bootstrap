# If BASH_ENV variable is set when Bash is invoked to execute a shell script, its value is expanded and used as
# the name of a startup file to read before executing the script.
export BASH_ENV=$HOME/hrt/boot/conf/bash/env.bashrc
